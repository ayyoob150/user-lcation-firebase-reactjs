import React from "react";
import Logo from "./Logo";

const Header = () => {
  return (
    <header>
      <Logo />
      {/* You can add any other header elements here */}
    </header>
  );
};

export default Header;